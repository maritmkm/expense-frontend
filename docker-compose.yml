services:
  mongodb:
    image: mongo:latest
    container_name: expense-tracker-db
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: always

volumes:
  mongodb_data:
